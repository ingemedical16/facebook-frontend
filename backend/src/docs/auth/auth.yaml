/register:
  post:
    $ref: './register.yaml'
/verify-email:
  post:
    $ref: './verify-email.yaml'
/login:
  post:
    $ref: './login.yaml'
/sendResetPasswordCode:
  post:
    $ref: './send-reset-password-code.yaml'
/validateResetCode:
  post:
    $ref: './validate-reset-code.yaml'
/changePassword:
  post:
    $ref: './change-password.yaml'
